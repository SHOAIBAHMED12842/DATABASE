SELECT * FROM AUDIT_029;
CREATE OR REPLACE TRIGGER AUDIT_EMP 
AFTER DELETE OR INSERT OR UPDATE ON employee122 
FOR EACH ROW 
BEGIN 
IF DELETING THEN 
 	UPDATE 	AUDIT_029 	SET 	DEL = DEL + 1 
 	WHERE 	USER_NAME  = 'SCOTT' AND TABLE_NAME  = 'EMP' 
 	AND   	COLUMN_NAME  IS NULL; 
ELSIF INSERTING THEN 
 	UPDATE AUDIT_029 	SET 	INS = INS + 1 
 	WHERE 	USER_NAME  = 'SCOTT' AND TABLE_NAME  = 'EMP' 
 	AND   	COLUMN_NAME  IS NULL; 
ELSIF UPDATING('SAL') THEN 
 	UPDATE 	AUDIT_029 	SET 	UPD = UPD + 1 
WHERE 	USER_NAME  = 'SCOTT' AND TABLE_NAME  = 'EMP' 
 	AND   	COLUMN_NAME ='SAL'; 
ELSE 
 	UPDATE 	AUDIT_029 	SET 	UPD = UPD + 1 
WHERE 	USER_NAME  = 'SCOTT' AND TABLE_NAME  = 'EMP' 
 	AND   	COLUMN_NAME  IS NULL; 
END IF; 
END; 

INSERT INTO employee122 (EMPNO1, NAME1,DESIGNATION,QUALIFICATION,JOIN_DATE) VALUES(3, 'SHOAIB AHMMED',1, 'BE', TO_DATE('27-DEC-1980', 'DD-MON-YYYY'));

DROP TRIGGER EMP122; 
SELECT * FROM AUDIT_029;